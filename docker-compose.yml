services:
  front:
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:3002/api
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
